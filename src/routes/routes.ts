export enum AddRoutes
{
  ROOT = "/",
  ABOUT = "about",
  USERS = "users",
  COMPANIES = "/companies",
  REGISTER = "/register",
  LOGIN = "/login"
};

export enum ApiRoutes
{
  USERS_ME = "/users/me"
}
