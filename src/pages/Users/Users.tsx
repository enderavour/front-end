import { Container, Typography } from "@mui/material";

const Users = () => {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h3">
        Users
      </Typography>
    </Container>
  )
};

export default Users;
