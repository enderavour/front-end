import { Container, Typography } from "@mui/material";

const Companies = () => {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h3">
        Companies
      </Typography>
    </Container>
  )
};

export default Companies;
